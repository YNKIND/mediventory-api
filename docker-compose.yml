services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: mediventory
      POSTGRES_PASSWORD: localdevpassword
      POSTGRES_DB: mediventory
    ports:
      - "5432:5432"
    volumes:
      - mediventory_data:/var/lib/postgresql/data

volumes:
  mediventory_data: